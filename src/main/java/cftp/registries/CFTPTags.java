package cftp.registries;

import cftp.CFTP;
import net.minecraft.item.Item;
import net.minecraft.registry.RegistryKeys;
import net.minecraft.registry.tag.TagKey;
import net.minecraft.util.Identifier;

public class CFTPTags {

    //public static final TagKey<Item> GUNS_TAG = TagKey.of(RegistryKeys.ITEM, new Identifier("my_mod", "guns"));

    //public static final TagKey<Item> CHARGES = of("charges");
    //
    //private static TagKey<Item> of(String id) {
    //    return TagKey.of(RegistryKeys.ITEM, Identifier.of(CFTP.MOD_ID, id));
    //}

}
